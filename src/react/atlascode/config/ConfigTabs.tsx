import React from 'react';
type ConfigTabsProps = {
    tabContainer: React.ReactElement;
};

export const ConfigTabs: React.FunctionComponent<ConfigTabsProps> = ({ tabContainer }) => {
    return <>{tabContainer}</>;
};
